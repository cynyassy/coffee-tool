services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: coffee
      POSTGRES_PASSWORD: coffee
      POSTGRES_DB: coffee_tools
    ports:
      - "5432:5432"
    volumes:
      - coffee_pg:/var/lib/postgresql/data

volumes:
  coffee_pg:
